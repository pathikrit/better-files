machine:
  java:
    version: oraclejdk8
test:
  override:
    - sbt +clean +coverage +test
  post:
    - pip install --user codecov && codecov
#    - sbt coverageReport
#    - sbt coverageAggregate
#    - sbt codacyCoverage
